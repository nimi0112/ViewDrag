<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drag.view.view_drag">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Swipe.Back.NoActionBar">
        <activity android:name="com.drag.view.view_drag.CommonActivity">

        </activity>
        <activity android:name="com.drag.view.view_drag.secndMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.drag.view.view_drag.sampleActivity"></activity>
    </application>

</manifest>